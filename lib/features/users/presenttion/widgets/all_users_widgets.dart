/*
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';
import 'package:get/get.dart';
import 'package:loan_app/features/users/domain/entities/all_users.dart';

import '../../../loan/presenttion/pages/loan_screen.dart';

class AllUsersWidget extends StatelessWidget {
 AllUsers allUsers;
 int index=0;
AllUsersWidget(this.allUsers);
  @override
  Widget build(BuildContext context) {
    return InkWell(
      onTap: (() => Get.to(LoanScreen())),
      child: Container(
        width: 335.w,
        height: 97.5.h,
        child: Card(

          child: ListTile(
            leading: Image.asset(
              allUsers.data[index].image,
              width: 65.w,
              height: 65.h,
            ),
            title: Text(allUsers.data[index].first_name+" "+allUsers.data[index].last_name,style: context.theme.textTheme.bodyText1,),
            subtitle: Text(allUsers.data[index].phone_number,style: context.theme.textTheme.headline2,),
          ),
        ),
      ),*/
/**//*

    );
  }
}
*/
